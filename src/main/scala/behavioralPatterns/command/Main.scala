package behavioralPatterns.command

import javafx.application.Application

// Simple drawing application.

object Main {
  def main(args: Array[String]): Unit = {
    Application.launch(classOf[AppMain], null)
  }
}
